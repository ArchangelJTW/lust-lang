struct Factor
    base: int
    multiplier: int
end

extern
    function Factor:apply(self: Factor, value: int): int
    function host_double(value: int): int
    function make_factor(base: int, multiplier: int): Factor
end
